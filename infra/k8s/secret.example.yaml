apiVersion: v1
kind: Secret
metadata:
  name: nexus-secrets
  namespace: nexus
type: Opaque
stringData:
  NEXUS_SECRET_KEY: "replace-with-64-character-random-secret"
  NEXUS_DATABASE_URL: "postgresql+psycopg://nexus:replace@postgres:5432/nexus"
  NEXUS_REDIS_URL: "redis://redis:6379/0"

