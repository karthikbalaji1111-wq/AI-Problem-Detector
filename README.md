# NEXUS

The Autonomous AI Workforce.

Run locally:

```bash
docker compose up --build
```

Open the web app at `http://localhost:3000` and the API at `http://localhost:8000`.

Default seeded login:

- Email: `founder@nexus.dev`
- Password: `NexusPass123!`
